ALTER TABLE public.empresas
    ALTER COLUMN ruc TYPE VARCHAR(40),
    ADD COLUMN IF NOT EXISTS tipo_documento_fiscal VARCHAR(30) NOT NULL DEFAULT 'RUC',
    ADD COLUMN IF NOT EXISTS nombre_comercial VARCHAR(180),
    ADD COLUMN IF NOT EXISTS direccion_fiscal VARCHAR(500),
    ADD COLUMN IF NOT EXISTS distrito VARCHAR(120),
    ADD COLUMN IF NOT EXISTS provincia VARCHAR(120),
    ADD COLUMN IF NOT EXISTS departamento VARCHAR(120),
    ADD COLUMN IF NOT EXISTS pais_codigo VARCHAR(2) NOT NULL DEFAULT 'PE',
    ADD COLUMN IF NOT EXISTS pais_nombre VARCHAR(100) NOT NULL DEFAULT 'Peru',
    ADD COLUMN IF NOT EXISTS correo_principal VARCHAR(180),
    ADD COLUMN IF NOT EXISTS telefono VARCHAR(40),
    ADD COLUMN IF NOT EXISTS celular VARCHAR(40),
    ADD COLUMN IF NOT EXISTS sitio_web VARCHAR(300),
    ADD COLUMN IF NOT EXISTS facebook VARCHAR(300),
    ADD COLUMN IF NOT EXISTS instagram VARCHAR(300),
    ADD COLUMN IF NOT EXISTS representante_nombre VARCHAR(180),
    ADD COLUMN IF NOT EXISTS representante_tipo_documento VARCHAR(30),
    ADD COLUMN IF NOT EXISTS representante_numero_documento VARCHAR(40),
    ADD COLUMN IF NOT EXISTS representante_cargo VARCHAR(120),
    ADD COLUMN IF NOT EXISTS representante_correo VARCHAR(180),
    ADD COLUMN IF NOT EXISTS representante_telefono VARCHAR(40),
    ADD COLUMN IF NOT EXISTS zona_horaria VARCHAR(80) NOT NULL DEFAULT 'America/Lima',
    ADD COLUMN IF NOT EXISTS idioma VARCHAR(20) NOT NULL DEFAULT 'es-PE',
    ADD COLUMN IF NOT EXISTS formato_fecha VARCHAR(20) NOT NULL DEFAULT 'DD/MM/YYYY',
    ADD COLUMN IF NOT EXISTS formato_hora VARCHAR(10) NOT NULL DEFAULT '24H',
    ADD COLUMN IF NOT EXISTS moneda_codigo VARCHAR(3) NOT NULL DEFAULT 'PEN',
    ADD COLUMN IF NOT EXISTS moneda_simbolo VARCHAR(10) NOT NULL DEFAULT 'S/';
